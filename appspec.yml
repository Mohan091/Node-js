version: 0.0
os: linux 

files:
    - source: / 
      destination: /home/ubuntu/back/node-express-realworld-example-app
      timeout: 300
     
      


hooks:
   
   ApplicationInstall:
         - location: /sript/install_script.sh
           timeout: 300
           runas: root
           
   ApplicationStart:
         - location: /sript/start_script.sh
           timeout: 300
           runas: root
